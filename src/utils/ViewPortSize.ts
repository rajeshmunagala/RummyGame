export class ViewPortSize {
    public static MAX_WIDTH = window.innerWidth;
    public static MAX_HEIGHT = window.innerHeight;
    public static HALF_WIDTH = window.innerWidth / 2;
    public static HALF_HEIGHT = window.innerHeight / 2;
}
